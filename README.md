# teacher_antigravity

A Python development project.

## Environment Setup

This project uses a virtual environment. To set it up and activate it:

```bash
python3 -m venv venv
source venv/bin/activate
```

## Standards

- Always use a `venv` for each project folder.
- Maintain a clean `.gitignore`.
